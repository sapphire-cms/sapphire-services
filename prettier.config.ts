import type { Config } from 'prettier';

const config: Config = {
  semi: true,
  singleQuote: true,
  printWidth: 100,
  trailingComma: 'all',
  tabWidth: 2,
  endOfLine: 'lf',
};

export default config;
